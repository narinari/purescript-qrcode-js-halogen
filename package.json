{
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && eslint src && pulp build",
    "test": "pulp browserify --optimise -I example/src --to example/example.js"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "eslint": "^3.0.1",
    "jshint": "^2.9.1",
    "pulp": "^12.0.1",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.5.0"
  },
  "dependencies": {}
}
